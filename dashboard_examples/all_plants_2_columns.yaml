type: custom:auto-entities
card:
  type: grid
  columns: 2
  square: false
card_param: cards
filter:
  include:
    - domain: plant
      options:
        type: custom:flower-card
        display_type: compact # Optionnel, pour un affichage plus serré en 2 colonnes
sort:
  method: state # Trie par état (problème en premier)
  reverse: true
